[project]
name = "wbquant"
version = "0.2.1"
description = "Offline Western blot quantification with a PySide6 desktop GUI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["PySide6==6.10.2"]

[tool.uv]
package = false

[dependency-groups]
build = [
    "pyinstaller==6.19.0",
]
